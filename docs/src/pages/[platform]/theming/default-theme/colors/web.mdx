import { Fragment } from '@/components/Fragment';
import { ColorTokenGroups } from '@/components/Theming/ColorTokenGroups';

## Semantic

<Fragment>{({ platform }) => import(`./colorUsage.${platform}.mdx`)}</Fragment>

<ColorTokenGroups />
